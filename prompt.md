```sql
请你扮演一名前端开发专家，为我生成 [数量] 个用于发布到小红书3的内容卡片。

【结构要求】
1. 每一个卡片都必须是一个 <div> 元素。
2. 这个 <div> 必须包含 class="card"。
3. 这个 <div> 还必须有一个 data-card-index 属性，其值从 0 开始依次递增。
4. 卡片内部的内容可以自由发挥，但必须包含标题和正文。
【内容示例】 (这里可以贴上你想让 AI 发挥的主题，比如“关于 Puppeteer 的 5 个技巧”)

请严格按照上述结构要求生成完整的 HTML 代码。


 node generate.js
```
